services:
  web:
    build: .
    container_name: web_app_variant_19
    ports:
      - "8000:5000"
    dns:
      - 8.8.8.8
    depends_on:
      - redis
    restart: on-failure

  redis:
    image: redis:alpine
    container_name: redis_db_variant_19
    restart: on-failure